# MCDA Core v0.4 执行计划 v2.0

**版本类型**: Feature Release (功能版本)
**创建日期**: 2026-02-01
**预计工期**: 7 周 (33.5 人日)
**当前状态**: ⏳ 待开始

---

## 📋 版本概述

v0.4 是 MCDA Core 的第二个主要功能版本,在 v0.3 基础上扩展高级功能。

**核心目标**:
1. ✅ 新增标准化方法 (Logarithmic, Sigmoid)
2. ✅ 新增赋权方法 (CRITIC, 变异系数法)
3. ✅ 新增汇总算法 (TODIM, ELECTRE-I)
4. ✅ 完整测试覆盖 (目标 95%+)

**关键指标**:
- 算法数量: 5 → 9 (+4 个)
- 代码行数: ~3,800 → ~5,500 (+1,700 行)
- 测试数量: 468 → 650+ (+180+ 个)
- 测试覆盖率: 95% → 95%+

---

## 🎯 范围调整说明

### 调整原则 (v2.0)

**相比原计划的主要调整**:

1. **移除重复/低价值功能**:
   - ❌ SAW 算法 (与 WSM 重复)
   - ❌ Max/Threshold 标准化 (使用场景少)
   - ❌ 标准离差法 (与变异系数法重复)

2. **推迟到 v0.5**:
   - ⏭️ MACBETH 赋权 (需要复杂偏好处理)
   - ⏭️ MOORA 算法 (需要更多标准化方法支持)
   - ⏭️ ORESTE 算法 (需要特殊距离计算)
   - ⏭️ 德尔菲法 (需要迭代流程)
   - ⏭️ PCA 主成分分析 (特殊场景)

3. **增强测试**:
   - ✅ 测试时间: 2 → 4 人日 (+100%)
   - ✅ 集成测试: 新增跨算法测试
   - ✅ 性能测试: 新增大数据集测试

**调整后工作量**:
- 原计划: 31 人日
- 调整后: **33.5 人日** (+8%)
- **实际开发效率提升**: 聚焦高价值算法,减少上下文切换

---

## 📊 开发阶段

### Phase 1: 标准化方法扩展 (3.5 人日)

| 任务 | 工时 | 优先级 | 依赖 |
|------|------|--------|------|
| 1.1 Logarithmic 标准化 | 1.5 人日 | P0 | 无 |
| 1.2 Sigmoid 标准化 | 1.5 人日 | P0 | 无 |
| 1.3 测试与文档 | 0.5 人日 | P0 | 1.1, 1.2 |

**详细任务**:

#### 1.1 Logarithmic 标准化 (1.5 人日)

**目标**: 实现对数标准化,适用于比率型数据

**数学模型**:
```
对于效益型准则:
x'_ij = log(x_ij) / log(max(x_j))

对于成本型准则:
x'_ij = log(max(x_j)) / log(x_ij)
```

**实现要点**:
- 处理零值和负值 (加常数偏移)
- 对数底数选择 (natural log vs log10)
- 边界条件处理

**测试用例**:
```python
def test_logarithmic_positive():
    """测试：正数数据"""
    data = np.array([1, 10, 100, 1000])
    result = logarithmic_normalize(data, maximize=True)
    assert result.min() >= 0
    assert result.max() <= 1

def test_logarithmic_with_zeros():
    """测试：包含零值"""
    data = np.array([0, 1, 10, 100])
    # 加偏移量 log(x + 1)
    result = logarithmic_normalize(data, maximize=True)

def test_logarithmic_cost_benefit():
    """测试：效益型 vs 成本型"""
    data = np.array([10, 50, 100, 200])
    result_benefit = logarithmic_normalize(data, maximize=True)
    result_cost = logarithmic_normalize(data, maximize=False)
    # 反向关系
```

#### 1.2 Sigmoid 标准化 (1.5 人日)

**目标**: 实现 Sigmoid 标准化,适用于需要平滑转换的场景

**数学模型**:
```
对于效益型准则:
x'_ij = 1 / (1 + exp(-k * (x_ij - μ) / σ))

其中:
- k: 压缩系数 (推荐 2-5)
- μ: 均值
- σ: 标准差
```

**实现要点**:
- 自动计算 μ, σ
- 可调节压缩系数 k
- 数值稳定性 (exp() 溢出)

**测试用例**:
```python
def test_sigmoid_s_curve():
    """测试：S 曲线特性"""
    data = np.array([-10, -5, 0, 5, 10])
    result = sigmoid_normalize(data)
    # 验证 S 形状
    assert result[0] < result[2] < result[4]

def test_sigmoid_coefficient():
    """测试：不同压缩系数"""
    data = np.array([0, 5, 10])
    result_k2 = sigmoid_normalize(data, k=2)
    result_k5 = sigmoid_normalize(data, k=5)
    # k 越大,曲线越陡峭
```

**交付物**:
- ✅ `lib/normalization/logarithmic_normalizer.py`
- ✅ `lib/normalization/sigmoid_normalizer.py`
- ✅ 单元测试 (覆盖率 >95%)
- ✅ API 文档

---

### Phase 2: 赋权方法扩展 (4.5 人日)

| 任务 | 工时 | 优先级 | 依赖 |
|------|------|--------|------|
| 2.1 CRITIC 方法 | 2.5 人日 | P0 | 无 |
| 2.2 变异系数法 | 1.5 人日 | P0 | 无 |
| 2.3 测试与文档 | 0.5 人日 | P0 | 2.1, 2.2 |

**详细任务**:

#### 2.1 CRITIC 方法 (2.5 人日)

**目标**: 实现基于对比强度和冲突性的权重计算

**数学模型**:
```
1. 标准化决策矩阵
2. 计算标准差 (对比强度):
   σ_j = sqrt(Σ(x'_ij - μ'_j)² / (m-1))

3. 计算相关系数 (冲突性):
   r_jk = correlation(j, k)

4. 计算信息量:
   C_j = σ_j * Σ(1 - r_jk)

5. 归一化权重:
   w_j = C_j / Σ(C_j)
```

**实现要点**:
- 相关系数矩阵计算
- 对称矩阵处理
- 数值精度

**测试用例**:
```python
def test_critic_basic():
    """测试：基本功能"""
    matrix = np.array([
        [10, 5, 8],
        [8, 7, 6],
        [9, 6, 7],
    ])
    weights = critic_weighting(matrix)
    assert len(weights) == 3
    assert_allclose(sum(weights), 1.0)

def test_critic_correlation():
    """测试：相关性处理"""
    # 高相关性准则 -> 低权重
    matrix = np.array([
        [10, 10, 5],   # C1, C2 高度相关
        [8, 8, 7],
        [9, 9, 6],
    ])
    weights = critic_weighting(matrix)
    # w1 ≈ w2, 但由于冗余,值应该较小
```

#### 2.2 变异系数法 (1.5 人日)

**目标**: 基于数据离散程度的权重计算

**数学模型**:
```
1. 计算均值: μ_j = Σ(x_ij) / m
2. 计算标准差: σ_j = sqrt(Σ(x_ij - μ_j)² / (m-1))
3. 计算变异系数: CV_j = σ_j / μ_j
4. 归一化权重: w_j = CV_j / Σ(CV_j)
```

**实现要点**:
- 零均值处理
- 负值处理
- 相对变异度量

**测试用例**:
```python
def test_coefficient_of_variation():
    """测试：基本功能"""
    matrix = np.array([
        [10, 50, 8],
        [8, 30, 6],
        [9, 70, 7],
    ])
    weights = cv_weighting(matrix)
    # C2 变异最大,权重最高
    assert weights[1] > weights[0]
    assert weights[1] > weights[2]
```

**交付物**:
- ✅ `lib/weighting/critic_weighting.py`
- ✅ `lib/weighting/cv_weighting.py`
- ✅ 单元测试 (覆盖率 >95%)
- ✅ API 文档

---

### Phase 3: TODIM 算法实现 (5 人日)

**优先级**: P0 (高价值前景理论算法)

| 任务 | 工时 | 优先级 | 依赖 |
|------|------|--------|------|
| 3.1 核心算法实现 | 2.5 人日 | P0 | 无 |
| 3.2 排名计算 | 1.5 人日 | P0 | 3.1 |
| 3.3 测试与文档 | 1 人日 | P0 | 3.1, 3.2 |

**详细任务**:

#### 3.1 核心算法 (2.5 人日)

**目标**: 实现基于前景理论的 TODIM 算法

**数学模型**:
```
1. 计算相对测度:
   φ_c(a_i, a_j) = {
       sqrt(w_c * (x_ic - x_jc) / Σw_c)           if x_ic > x_jc
       -sqrt(Σw_c / w_c * (x_ic - x_jc) / Σw_c) / θ  otherwise
   }

2. 计算优势度:
   δ(a_i, a_j) = Σ φ_c(a_i, a_j)

3. 计算全局优势:
   ξ(a_i) = Σ δ(a_i, a_j) - Σ δ(a_j, a_i)

4. 排序: ξ(a_i) 越大越好
```

**实现要点**:
- 前景理论参数 θ (衰减系数, 推荐 1-2.5)
- 矩阵化计算优化
- 支持部分/不完全偏好

**测试用例**:
```python
def test_todim_basic():
    """测试：基本功能"""
    problem = DecisionProblem(
        alternatives=("A", "B", "C"),
        criteria=(...),
        scores={...}
    )
    result = todim(problem, theta=1.0)
    assert len(result.rankings) == 3

def test_todim_theta_sensitivity():
    """测试：θ 参数敏感性"""
    result_theta1 = todim(problem, theta=1.0)
    result_theta2 = todim(problem, theta=2.5)
    # 不同 θ 可能改变排名
```

#### 3.2 排名计算 (1.5 人日)

**要点**:
- 全局优势度排序
- 处理相同优势度
- 生成 DecisionResult

**交付物**:
- ✅ `lib/algorithms/todim.py`
- ✅ 单元测试 (覆盖率 >95%)
- ✅ 使用示例

---

### Phase 4: ELECTRE-I 算法实现 (7 人日)

**优先级**: P0 (级别优于关系经典算法)

| 任务 | 工时 | 优先级 | 依赖 |
|------|------|--------|------|
| 4.1 和谐指数计算 | 2 人日 | P0 | 无 |
| 4.2 不和谐指数计算 | 2 人日 | P0 | 4.1 |
| 4.3 可信度矩阵 | 1.5 人日 | P0 | 4.1, 4.2 |
| 4.4 排序与核提取 | 1.5 人日 | P0 | 4.3 |

**详细任务**:

#### 4.1 和谐指数 (2 人日)

**数学模型**:
```
1. 加权和谐指数:
   C(a_i, a_j) = Σ w_c * I_c(a_i, a_j) / Σ w_c

2. 指示函数:
   I_c(a_i, a_j) = {
       1    if x_ic > x_jc
       0    if x_ic < x_jc
       0.5  if x_ic = x_jc
   }
```

**实现要点**:
- 权重归一化
- 准则方向处理
- 和谐性阈值设定

#### 4.2 不和谐指数 (2 人日)

**数学模型**:
```
d_c(a_i, a_j) = {
    0                              if x_ic >= x_jc
    (x_jc - x_ic) / max_range_c   otherwise
}
```

**实现要点**:
- 最大范围计算
- 不和谐性阈值
- 模糊版本支持

#### 4.3 可信度矩阵 (1.5 人日)

**数学模型**:
```
S(a_i, a_j) = {
    C(a_i, a_j)                    if d_c(a_i, a_j) ≤ D_c
    C(a_i, a_j) * Π((1-d_c)/(1-D_c))  otherwise
}
```

#### 4.4 排序与核提取 (1.5 人日)

**要点**:
- 级别优于关系构建
- 核 (Kernel) 提取算法
- 非被优方案集合

**测试用例**:
```python
def test_electre_i_kernel():
    """测试：核提取"""
    problem = DecisionProblem(...)
    result = electre_i(
        problem,
        concordance_threshold=0.7,
        discordance_threshold=0.3
    )
    # 核中的方案不被任何方案优超
    kernel = result.kernel
    verify_kernel_property(kernel, problem)

def test_electre_i_thresholds():
    """测试：阈值敏感性"""
    result_strict = electre_i(problem, c_h=0.8, d_h=0.2)
    result_loose = electre_i(problem, c_h=0.6, d_h=0.4)
    # 严格阈值 -> 更小的核
    assert len(result_strict.kernel) <= len(result_loose.kernel)
```

**交付物**:
- ✅ `lib/algorithms/electre_i.py`
- ✅ 单元测试 (覆盖率 >95%)
- ✅ 算法论文参考

---

### Phase 5: 测试与集成 (7.5 人日)

| 任务 | 工时 | 优先级 | 依赖 |
|------|------|--------|------|
| 5.1 单元测试完善 | 2 人日 | P0 | 全部开发 |
| 5.2 集成测试 | 2 人日 | P0 | 5.1 |
| 5.3 性能测试 | 1.5 人日 | P1 | 5.1 |
| 5.4 文档更新 | 1.5 人日 | P0 | 5.1 |
| 5.5 完整测试运行 | 0.5 人日 | P0 | 全部 |

#### 5.1 单元测试 (2 人日)

**目标**: 95%+ 测试覆盖率

**新增测试**:
- 标准化方法: 30+ 测试
- 赋权方法: 40+ 测试
- TODIM: 35+ 测试
- ELECTRE-I: 45+ 测试

#### 5.2 集成测试 (2 人日)

**测试场景**:
```python
def test_end_to_end_todim():
    """测试：完整 TODIM 工作流"""
    # 1. 加载数据
    problem = load_yaml("examples/todim_case.yaml")
    # 2. 运行算法
    result = todim(problem, theta=1.5)
    # 3. 验证结果
    assert result.is_valid()
    # 4. 生成报告
    report = generate_markdown_report(result)

def test_cross_algorithm_comparison():
    """测试：跨算法对比"""
    algorithms = ["wsm", "topsis", "todim", "electre_i"]
    results = compare_algorithms(problem, algorithms)
    # Spearman 相关系数
    correlations = compute_ranking_correlation(results)

def test_weighting_method_impact():
    """测试：赋权方法影响"""
    weight_methods = ["ahp", "entropy", "critic", "cv"]
    for method in weight_methods:
        weights = compute_weights(problem, method)
        result_topsis = topsis(problem, weights=weights)
        # 分析权重变化对排名的影响
```

#### 5.3 性能测试 (1.5 人日)

**测试场景**:
```python
def test_large_dataset_performance():
    """测试：大数据集性能"""
    # 1000 个方案, 50 个准则
    problem = generate_large_problem(
        alternatives=1000,
        criteria=50
    )
    start = time.time()
    result = todim(problem)
    duration = time.time() - start
    # 目标: < 5 秒
    assert duration < 5.0

def test_scalability():
    """测试：可扩展性"""
    sizes = [10, 50, 100, 500, 1000]
    times = []
    for n in sizes:
        problem = generate_problem(n_alternatives=n)
        t = time_algorithm(todim, problem)
        times.append(t)
    # 验证复杂度: O(n²)
    verify_complexity(times, expected=2)
```

#### 5.4 文档更新 (1.5 人日)

**更新内容**:
1. README.md:
   - 新算法列表
   - 使用示例
   - 快速开始

2. API 文档:
   - 所有新 API
   - 参数说明
   - 返回值格式

3. 示例代码:
   - TODIM 完整案例
   - ELECTRE-I 完整案例
   - CRITIC 赋权案例

**交付物**:
- ✅ 更新后的 README
- ✅ API 参考文档
- ✅ 3+ 个完整示例

#### 5.5 完整测试运行 (0.5 人日)

**验收标准**:
```bash
# 运行全部测试
pytest tests/ -v --cov=mcda_core --cov-report=html

# 验收条件
✅ 总测试数: 650+ 个
✅ 通过率: 100%
✅ 覆盖率: 95%+
✅ 执行时间: < 10 秒
```

---

## 📈 进度跟踪

### 开发时间表 (7 周)

| 周次 | 阶段 | 交付物 | 状态 |
|------|------|--------|------|
| W1 | Phase 1 | 标准化方法 | ⏳ 待开始 |
| W2-W3 | Phase 2 | 赋权方法 | ⏳ 待开始 |
| W3-W4 | Phase 3 | TODIM 算法 | ⏳ 待开始 |
| W4-W6 | Phase 4 | ELECTRE-I 算法 | ⏳ 待开始 |
| W7 | Phase 5 | 测试与文档 | ⏳ 待开始 |

### 验收标准

**功能完整性**:
- ✅ 4 个新算法全部实现
- ✅ 2 个新赋权方法全部实现
- ✅ 2 个新标准化方法全部实现

**质量标准**:
- ✅ 测试覆盖率 ≥ 95%
- ✅ 所有测试通过 (650+ 个)
- ✅ 性能: 1000 方案 < 5 秒

**文档完整性**:
- ✅ README 更新
- ✅ API 文档完整
- ✅ 3+ 个完整示例

---

## ⚠️ 风险管理

### 风险 1: ELECTRE-I 复杂度超预期

**概率**: 中 (40%)
**影响**: 高 (延迟 3-5 天)

**缓解措施**:
- 优先实现简化版本 (确定版本)
- 模糊版本推迟到 v0.5
- 增加单元测试覆盖

**应急计划**:
- 如果 Week 6 结束时未完成,标记为 EXPERIMENTAL
- v0.4.1 补丁版本完成完整功能

### 风险 2: TODIM 性能问题

**概率**: 低 (20%)
**影响**: 中 (延迟 1-2 天)

**缓解措施**:
- 使用 NumPy 矩阵运算
- 避免嵌套循环
- 性能测试驱动优化

### 风险 3: 测试覆盖率不足

**概率**: 低 (15%)
**影响**: 低 (延长 1-2 天)

**缓解措施**:
- TDD 方法论强制
- 每个功能至少 15 个测试
- 持续验证覆盖率

---

## 🎓 参考资源

### 算法论文

1. **TODIM**:
   - Gomes, L. A. M. M., & Lima, M. M. P. P. (1992)
   - TODIM: Basics and application to multicriteria ranking of projects

2. **ELECTRE-I**:
   - Roy, B. (1968)
   - Classement et choix en présence de points de vue multiples

3. **CRITIC**:
   - Diakoulaki, D., Mavrotas, G., & Papayannakis, L. (1995)
   - Determining objective weights in multiple criteria problems

### 技术文档

- `docs/decisions/mcda-core/` - ADR 架构决策
- `docs/checkpoints/mcda-core/checkpoint-v0.3.1.md` - v0.3.1 里程碑
- `README_CN.md` - 使用文档

---

## 🚀 下一步行动

**立即开始**:
1. 创建 `docs/active/mcda-core/v0.4/tdd-todim.md`
2. 创建 `docs/active/mcda-core/v0.4/development-progress.md`
3. 开始 Phase 1: Logarithmic 标准化

**本周目标**:
- ✅ 完成 Logarithmic 标准化
- ✅ 完成 Sigmoid 标准化
- ✅ 通过所有单元测试

---

**最后更新**: 2026-02-01
**维护者**: AI (Claude Sonnet 4.5) + 人工贡献者
**版本**: v2.0 (调整范围)
