# 一票否决机制 TDD 进度

**版本**: v0.10
**功能**: 一票否决机制（Veto Mechanism）
**开始日期**: 待定
**开发方法**: TDD（测试驱动开发）
**状态**: 📋 待开始

---

## 🎯 功能概述

实现 MCDA Core 的一票否决机制，支持：
- **hard** 硬否决：不满足条件直接排除
- **soft** 软否决：不满足条件扣分惩罚
- **tiered** 分级否决：多档位管理
- **composite** 组合否决：AND/OR 逻辑组合

---

## 📋 RED Phase（编写失败测试）

### 测试文件: `test_models.py`

#### VetoCondition 测试
- [ ] `test_veto_condition_equals_operator()`
  - 测试等于操作符 (`==`)
  - 验证正确评估
- [ ] `test_veto_condition_greater_than_operator()`
  - 测试大于操作符 (`>`)
  - 验证正确评估
- [ ] `test_veto_condition_less_than_operator()`
  - 测试小于操作符 (`<`)
  - 验证正确评估
- [ ] `test_veto_condition_in_operator()`
  - 测试包含操作符 (`in`)
  - 验证正确评估
- [ ] `test_veto_condition_invalid_operator_raises_error()`
  - 测试无效操作符抛出错误
- [ ] `test_veto_condition_with_interval_value()`
  - 测试区间数值评估

#### VetoConfig 测试
- [ ] `test_veto_config_hard_type()`
  - 测试硬否决配置
- [ ] `test_veto_config_soft_type()`
  - 测试软否决配置
- [ ] `test_veto_config_tiered_type()`
  - 测试分级否决配置
- [ ] `test_veto_config_composite_type()`
  - 测试组合否决配置
- [ ] `test_veto_config_validation()`
  - 测试配置验证逻辑
- [ ] `test_veto_config_invalid_type_raises_error()`
  - 测试无效类型抛出错误

#### VetoResult 测试
- [ ] `test_veto_result_creation()`
  - 测试结果创建
- [ ] `test_veto_result_rejected()`
  - 测试被拒绝方案
- [ ] `test_veto_result_warning()`
  - 测试有警告方案
- [ ] `test_veto_result_with_details()`
  - 测试详细信息

---

### 测试文件: `test_evaluator.py`

#### 硬否决测试
- [ ] `test_hard_veto_accept()`
  - 期望：方案通过硬否决检查
  - 输入：资质评分 80，阈值 60
- [ ] `test_hard_veto_reject()`
  - 期望：方案被硬否决拒绝
  - 输入：资质评分 40，阈值 60
- [ ] `test_hard_veto_with_lower_better()`
  - 期望：反向指标正确评估
  - 输入：负债率 70，阈值 50（higher_better 被拒绝）
- [ ] `test_hard_veto_multiple_criteria()`
  - 期望：任一不满足即拒绝（OR 逻辑）
  - 输入：3 个准则，2 个通过 1 个不通过

#### 软否决测试
- [ ] `test_soft_veto_no_penalty()`
  - 期望：不触发惩罚
  - 输入：财务风险 50，阈值 60
- [ ] `test_soft_veto_with_penalty()`
  - 期望：触发惩罚扣分
  - 输入：财务风险 70，阈值 60，惩罚 -30 分
- [ ] `test_soft_veto_accumulates_penalties()`
  - 期望：多个软否决惩罚累计
  - 输入：2 个准则都触发，累计扣 50 分
- [ ] `test_soft_veto_penalty_does_not_reject()`
  - 期望：软否决不拒绝方案，只扣分
  - 输入：财务风险 90，扣 50 分后仍参与排序

#### 分级否决测试
- [ ] `test_tiered_veto_low_risk_accept()`
  - 期望：低风险接受
  - 输入：技术风险 25，低风险档位
- [ ] `test_tiered_veto_medium_risk_warning()`
  - 期望：中风险警告（扣分）
  - 输入：技术风险 45，中风险档位，扣 15 分
- [ ] `test_tiered_veto_high_risk_reject()`
  - 期望：高风险拒绝
  - 输入：技术风险 70，高风险档位
- [ ] `test_tiered_veto_boundary_cases()`
  - 期望：边界值正确判断
  - 输入：阈值 30，值 30 应该接受
- [ ] `test_tiered_veto_multiple_tiers()`
  - 期望：多档位正确评估
  - 输入：3 个档位配置

#### 组合否决测试
- [ ] `test_composite_veto_or_logic_one_match()`
  - 期望：OR 逻辑，任一条件满足即触发
  - 输入：3 个条件，1 个满足
- [ ] `test_composite_veto_or_logic_multiple_matches()`
  - 期望：OR 逻辑，多个条件满足取最严格
  - 输入：3 个条件，2 个满足
- [ ] `test_composite_veto_or_logic_no_match()`
  - 期望：OR 逻辑，都不满足则通过
  - 输入：3 个条件，都不满足
- [ ] `test_composite_veto_and_logic_all_match()`
  - 期望：AND 逻辑，所有条件满足才触发
  - 输入：3 个条件，都满足
- [ ] `test_composite_veto_and_logic_partial_match()`
  - 期望：AND 逻辑，部分满足不触发
  - 输入：3 个条件，2 个满足
- [ ] `test_composite_veto_mixed_actions()`
  - 期望：混合动作（警告 + 拒绝）
  - 输入：2 个条件，1 个警告 1 个拒绝

#### 集成测试
- [ ] `test_evaluator_integration()`
  - 期望：完整评估流程
  - 输入：多个准则，多种否决类型

---

### 测试文件: `test_constraint_service.py`

#### 服务层测试
- [ ] `test_filter_problem_no_veto_rules()`
  - 期望：无否决规则，原样返回
- [ ] `test_filter_problem_with_hard_veto()`
  - 期望：过滤掉被硬否决的方案
- [ ] `test_filter_problem_all_rejected()`
  - 期望：所有方案被拒绝，返回原问题
- [ ] `test_filter_problem_partial_rejected()`
  - 期望：部分方案被拒绝
- [ ] `test_apply_penalties_to_scores()`
  - 期望：应用软否决惩罚分数
- [ ] `test_apply_penalties_with_soft_veto()`
  - 期望：软否决惩罚正确应用
- [ ] `test_get_constraint_metadata()`
  - 期望：返回正确的元数据
- [ ] `test_service_integration_with_algorithm()`
  - 期望：与算法集成正常工作

---

### 测试文件: 集成测试

#### 场景测试
- [ ] `test_vendor_qualification_scenario()`
  - **场景**: 供应商准入评估
  - **准则**: 营业执照（硬否决）、资质等级（硬否决）、财务风险（软否决）
  - **期望**: 2 个供应商被拒绝，2 个通过，1 个扣分
- [ ] `test_project_risk_assessment_scenario()`
  - **场景**: 项目风险评估
  - **准则**: 政策合规性（硬否决）、技术风险（分级否决）、资金风险（分级否决）
  - **期望**: 1 个项目被拒绝（政策），1 个被拒绝（高风险），1 个警告
- [ ] `test_contract_risk_scenario()`
  - **场景**: 合同风险评估
  - **准则**: 法律合规（硬否决）、付款条款（组合 OR）、违约责任（组合 AND）
  - **期望**: 1 个被拒绝（法律），1 个被拒绝（违约责任），1 个扣分

---

## 🟢 GREEN Phase（实现功能）

### 文件: `models.py`

#### VetoCondition 类
- [ ] `__init__()` - 初始化参数
- [ ] `__post_init__()` - 参数验证
- [ ] `evaluate()` - 评估条件
  - [ ] 支持 `==`, `!=`, `>`, `>=`, `<`, `<=`
  - [ ] 支持 `in`, `not_in`
  - [ ] 返回 `bool`

#### VetoConfig 类
- [ ] `__init__()` - 初始化配置
- [ ] `__post_init__()` - 配置验证
  - [ ] hard 类型必须有 condition
  - [ ] tiered 类型必须有 tiers
  - [ ] composite 类型必须有 conditions 和 logic

#### VetoTier 类
- [ ] `__init__()` - 初始化档位
- [ ] `__post_init__()` - 参数验证

#### VetoResult 类
- [ ] `__init__()` - 初始化结果
- [ ] `__post_init__()` - 结果验证

#### ConstraintMetadata 类
- [ ] `__init__()` - 初始化元数据

---

### 文件: `evaluator.py`

#### VetoEvaluator 类
- [ ] `__init__()` - 初始化评估器
- [ ] `evaluate()` - 主评估方法
  - [ ] 遍历所有带否决配置的准则
  - [ ] 调用对应的评估方法
  - [ ] 返回 VetoResult
- [ ] `_evaluate_criterion()` - 评估单个准则
- [ ] `_evaluate_hard()` - 硬否决评估
  - [ ] 检查条件是否满足
  - [ ] 返回 accept/reject
- [ ] `_evaluate_soft()` - 软否决评估
  - [ ] 检查条件是否满足
  - [ ] 返回 accept/warning + 惩罚分数
- [ ] `_evaluate_tiered()` - 分级否决评估
  - [ ] 遍历所有档位
  - [ ] 找到匹配的档位
  - [ ] 返回 accept/warning/reject
- [ ] `_evaluate_composite()` - 组合否决评估
  - [ ] 评估所有条件
  - [ ] 应用 AND/OR 逻辑
  - [ ] 返回结果
- [ ] `_action_to_result()` - 动作转结果字典

---

### 文件: `constraint_service.py`

#### ConstraintService 类
- [ ] `__init__()` - 初始化服务，创建 VetoEvaluator
- [ ] `filter_problem()` - 过滤决策问题
  - [ ] 收集带否决配置的准则
  - [ ] 评估所有方案
  - [ ] 过滤掉被拒绝的方案
  - [ ] 返回 (过滤后的问题, 否决结果)
- [ ] `apply_penalties()` - 应用惩罚分数
  - [ ] 遍历所有方案
  - [ ] 应用软否决惩罚
  - [ ] 返回调整后的问题
- [ ] `get_constraint_metadata()` - 获取元数据
  - [ ] 统计被拒绝/警告的方案
  - [ ] 返回 ConstraintMetadata
- [ ] `_create_filtered_problem()` - 创建过滤后的问题（私有方法）

---

### 文件: `models.py` 扩展

#### Criterion 类扩展
- [ ] 添加 `veto: VetoConfig | None = None` 字段
- [ ] 更新 `__post_init__()` 验证 veto 配置

#### DecisionResult 类扩展
- [ ] 添加 `veto_results: dict[str, VetoResult] = field(default_factory=dict)` 字段

---

## 🔄 REFACTOR Phase（重构优化）

### 代码优化
- [ ] 提取公共逻辑（条件评估、结果转换）
- [ ] 优化条件判断（减少嵌套）
- [ ] 完善错误处理（自定义异常）
- [ ] 添加类型注解（100% 覆盖）
- [ ] 优化性能（减少重复遍历）

### 文档完善
- [ ] 添加模块文档字符串
- [ ] 添加类文档字符串
- [ ] 添加方法文档字符串
- [ ] 添加示例代码

---

## ✅ 验收标准

### 功能验收
- [ ] 支持硬否决（直接排除）
- [ ] 支持软否决（扣分惩罚）
- [ ] 支持分级否决（多档位）
- [ ] 支持组合否决（AND/OR）
- [ ] 与现有算法无缝集成

### 质量验收
- [ ] 所有测试通过（100%）
- [ ] 测试覆盖率 ≥ 90%
- [ ] 代码符合 PEP 8 规范
- [ ] 类型注解 100% 覆盖
- [ ] 文档字符串完整

### 性能验收
- [ ] 评估 5 个方案 < 0.01 秒
- [ ] 评估 50 个方案 < 0.1 秒
- [ ] 内存占用合理

---

## 📊 进度跟踪

### 测试统计

| 测试类型 | 测试数 | 通过 | 失败 | 覆盖率 |
|----------|--------|------|------|--------|
| 数据模型测试 | 13 | 0 | 13 | 0% |
| 评估器测试 | 22 | 0 | 22 | 0% |
| 服务层测试 | 8 | 0 | 8 | 0% |
| 集成测试 | 3 | 0 | 3 | 0% |
| **总计** | **46** | **0** | **46** | **0%** |

### 工期统计

| Phase | 预计工期 | 实际工期 | 状态 |
|-------|----------|----------|------|
| RED Phase | 0.5人日 | - | 📋 待开始 |
| GREEN Phase | 1.5人日 | - | 📋 待开始 |
| REFACTOR Phase | 0.5人日 | - | 📋 待开始 |
| 文档和测试 | 0.5人日 | - | 📋 待开始 |
| **总计** | **3人日** | - | - |

---

## 📝 相关文档

### 架构设计
- [ADR-014: 一票否决机制架构设计](../../../decisions/mcda-core/014-veto-mechanism.md)
- [v0.10 执行计划](../../../plans/mcda-core/v0.10/execution-plan.md)

### 配置指南
- [一票否决配置指南](./veto-configuration-guide.md) - 待创建
- [使用案例](./veto-use-cases.md) - 待创建

---

**TDD 进度文件创建日期**: 2026-02-05
**创建人**: Claude Sonnet 4.5
**状态**: 📋 待开始

**下一步**: 等待用户确认后开始 RED Phase
