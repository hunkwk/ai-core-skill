# MCDA Core v0.12 执行计划 - 功能增强与性能优化版

**版本**: v0.12（合并版本）
**功能**: 性能测试和优化 + 报告进一步增强 + 高级可视化
**前序版本**: v0.10（一票否决）+ v0.11（报告导出增强）
**创建日期**: 2026-02-05
**状态**: 📋 计划中

---

## 📊 版本定位

### 版本目标

**v0.12: 功能增强与性能优化版**

这是一个综合版本，将原规划的 v0.12（性能测试）、v0.13（报告增强）、v0.14（高级可视化）合并为一个功能完整的版本，全面提升 MCDA Core 的性能、用户体验和可视化能力。

### 版本策略

```
v0.10 (一票否决) ✅
    ↓
v0.11 (报告导出增强) ✅
    ↓
v0.12 (功能增强与性能优化) 📋 当前版本
    ├─ Phase 1: 性能测试和优化
    ├─ Phase 2: 报告进一步增强
    ├─ Phase 3: 高级可视化
    └─ Phase 4: 集成测试和文档
    ↓
v0.13 (后续功能) 📋 计划中
    ↓
v1.0 (正式发布) 🚀 最终目标
```

### 合并原因

| 原因 | 说明 |
|------|------|
| **开发效率** | 三个版本功能相关，合并后减少上下文切换 |
| **用户价值** | 一次性获得完整的性能+可视化体验 |
| **测试协同** | 性能测试和可视化测试可以共享测试数据 |
| **发布节奏** | 7-10 人日的工期适合作为一个迭代周期 |

---

## 🎯 v0.12 核心目标

### 主要目标

1. **建立性能基准和优化**
   - 测量各种算法在不同规模数据下的性能
   - 建立性能基准数据
   - 识别并优化性能瓶颈

2. **报告系统增强**
   - 扩展图表类型（雷达图、折线图、热力图等）
   - 实现报告主题系统
   - 支持自定义报告模板

3. **高级可视化**
   - 敏感性分析热力图
   - 决策路径追踪图
   - 可选的交互式可视化

---

## 📋 功能分解

### Phase 1: 性能测试和优化（2-3 人日）

#### 目标

建立性能测试框架，识别性能瓶颈，优化关键算法。

#### 1.1 性能测试框架搭建（0.5 人日）

**创建目录**:
```
tests/mcda-core/performance/
├── __init__.py
├── test_large_scale.py         # 大规模数据测试
├── test_benchmarks.py          # 算法基准测试
├── test_memory_usage.py        # 内存使用测试
└── fixtures/                   # 性能测试数据
    ├── large_10x5.yaml
    ├── large_50x20.yaml
    ├── large_200x50.yaml
    └── large_1000x100.yaml
```

**文件**: `tests/mcda-core/performance/utils.py`

```python
"""
性能测试工具函数
"""

import time
import tracemalloc
from typing import Callable, Any
import psutil


def measure_execution_time(func: Callable, *args, **kwargs) -> dict[str, Any]:
    """测量函数执行时间和资源使用

    Returns:
        dict: {
            'execution_time': float,  # 执行时间（秒）
            'result': Any,             # 返回值
            'memory_mb': float         # 内存使用（MB）
        }
    """
    tracemalloc.start()
    process = psutil.Process()
    start_memory = process.memory_info().rss / 1024 / 1024  # MB

    start_time = time.perf_counter()
    result = func(*args, **kwargs)
    end_time = time.perf_counter()

    end_memory = process.memory_info().rss / 1024 / 1024  # MB
    current, peak = tracemalloc.get_traced_memory()
    tracemalloc.stop()

    return {
        'execution_time': end_time - start_time,
        'result': result,
        'memory_mb': end_memory - start_memory,
        'memory_peak_mb': peak / 1024 / 1024,
    }


class PerformanceCriteria:
    """性能验收标准"""

    # 响应时间标准（秒）
    RESPONSE_TIME_LIMITS = {
        'small': (10, 5, 0.5),      # 10方案 x 5准则
        'medium': (50, 20, 2.0),     # 50方案 x 20准则
        'large': (200, 50, 10.0),    # 200方案 x 50准则
        'xlarge': (1000, 100, 60.0), # 1000方案 x 100准则
    }

    # 内存使用标准（MB）
    MEMORY_LIMITS = {
        'small': 50,
        'medium': 200,
        'large': 1024,
        'xlarge': 4096,
    }

    @classmethod
    def check_performance(cls, category: str, alternatives: int,
                          criteria: int, exec_time: float,
                          memory_mb: float) -> dict[str, bool]:
        """检查性能是否符合标准

        Returns:
            dict: {
                'response_time_ok': bool,
                'memory_ok': bool,
                'category': str
            }
        """
        time_limit = cls.RESPONSE_TIME_LIMITS[category]
        memory_limit = cls.MEMORY_LIMITS[category]

        return {
            'response_time_ok': exec_time < time_limit[2],
            'memory_ok': memory_mb < memory_limit,
            'category': category,
            'time_limit': time_limit[2],
            'memory_limit': memory_limit,
        }
```

**验收标准**:
- [ ] 性能测试目录创建完成
- [ ] 工具函数实现并测试通过
- [ ] 测试数据生成器可用

#### 1.2 大规模数据测试（1 人日）

**测试场景**:

| 场景名称 | 方案数 | 准则数 | 预期响应时间 | 内存限制 |
|---------|--------|--------|--------------|----------|
| 小规模 | 10 | 5 | < 0.5s | < 50MB |
| 中规模 | 50 | 20 | < 2s | < 200MB |
| 大规模 | 200 | 50 | < 10s | < 1GB |
| 超大规模 | 1000 | 100 | < 60s | < 4GB |

**文件**: `tests/mcda-core/performance/test_large_scale.py`

```python
"""
大规模数据性能测试
"""

import pytest
from pathlib import Path
from mcda_core.core import MCDAOrchestrator
from .utils import measure_execution_time, PerformanceCriteria


class TestLargeScalePerformance:
    """大规模数据性能测试"""

    def test_small_scale_performance(self):
        """小规模性能测试: 10方案 x 5准则"""
        config_path = Path(__file__).parent / "fixtures" / "large_10x5.yaml"
        orchestrator = MCDAOrchestrator()

        result = measure_execution_time(
            orchestrator.run_workflow,
            config_path
        )

        criteria = PerformanceCriteria.check_performance(
            'small', 10, 5,
            result['execution_time'],
            result['memory_mb']
        )

        assert criteria['response_time_ok'], \
            f"响应时间 {result['execution_time']:.2f}s 超过限制"
        assert criteria['memory_ok'], \
            f"内存使用 {result['memory_mb']:.1f}MB 超过限制"

    def test_medium_scale_performance(self):
        """中规模性能测试: 50方案 x 20准则"""
        # 类似实现
        pass

    def test_large_scale_performance(self):
        """大规模性能测试: 200方案 x 50准则"""
        # 类似实现
        pass

    def test_xlarge_scale_performance(self):
        """超大规模性能测试: 1000方案 x 100准则"""
        # 类似实现
        pass

    def test_all_algorithms_large_scale(self):
        """所有算法大规模性能对比"""
        algorithms = ['topsis', 'todim', 'vikor', 'electre1', 'promethee2']

        for algo in algorithms:
            # 测试每种算法在大规模数据下的性能
            pass
```

**验收标准**:
- [ ] 4 个规模测试全部通过
- [ ] 响应时间符合预期
- [ ] 内存使用在限制内
- [ ] 所有算法测试通过

#### 1.3 算法性能基准测试（0.5 人日）

**文件**: `tests/mcda-core/performance/test_benchmarks.py`

**验收标准**:
- [ ] 所有算法基准测试完成
- [ ] 性能报告生成（Markdown + JSON）
- [ ] 基准数据可对比

#### 1.4 瓶颈分析和优化（1 人日）

**优化场景**:

##### 优化1: NumPy 矩阵运算
**文件**: `skills/mcda-core/lib/utils/array_ops.py`（新建）

```python
"""
矩阵运算优化工具
"""

import numpy as np
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from .models import DecisionProblem


def scores_to_numpy(problem: DecisionProblem) -> np.ndarray:
    """将评分字典转换为 NumPy 数组

    Args:
        problem: 决策问题

    Returns:
        np.ndarray: 形状为 (n_alternatives, n_criteria) 的数组
    """
    alternatives = sorted(problem.alternatives)
    criteria = sorted(problem.criteria, key=lambda c: c.name)

    scores_matrix = []
    for alt in alternatives:
        row = [problem.scores[alt][crit.name] for crit in criteria]
        scores_matrix.append(row)

    return np.array(scores_matrix, dtype=np.float64)
```

##### 优化2: 缓存机制
**文件**: `skills/mcda-core/lib/utils/cache.py`（新建）

```python
"""
简单的缓存装饰器
"""

from functools import lru_cache
from typing import Callable, TypeVar

T = TypeVar('T')


def cached_result(maxsize: int = 128):
    """缓存结果的装饰器"""
    def decorator(func: Callable[..., T]) -> Callable[..., T]:
        cached_func = lru_cache(maxsize=maxsize)(func)

        def wrapper(*args, **kwargs):
            return cached_func(*args, **kwargs)

        wrapper.cache_info = cached_func.cache_info
        wrapper.cache_clear = cached_func.cache_clear
        return wrapper

    return decorator
```

**验收标准**:
- [ ] 识别出 Top 5 性能瓶颈
- [ ] 关键瓶颈已优化
- [ ] 性能提升 >= 20%（相比优化前）
- [ ] 功能测试全部通过
- [ ] 无性能回归

---

### Phase 2: 报告进一步增强（2-3 人日）

#### 目标

扩展报告系统的图表类型和样式，实现主题系统和模板支持。

#### 2.1 更多图表类型（1 人日）

**新增图表类型**:

| 图表类型 | 用途 | 文件 |
|---------|------|------|
| 雷达图 | 多维度对比 | `charts.py` |
| 折线图 | 敏感性趋势 | `charts.py`（已有） |
| 热力图 | 准则相关性 | `charts.py` |
| 散点图 | 方案分布 | `charts.py` |
| 箱线图 | 评分分布 | `charts.py` |

**文件**: `skills/mcda-core/lib/visualization/charts.py`（扩展）

```python
class ChartGenerator:
    """图表生成器（扩展）"""

    def plot_radar(self, alternatives, criteria_scores, title="多维度对比"):
        """绘制雷达图（多维度对比）"""
        pass

    def plot_heatmap(self, matrix, row_labels, col_labels, title="热力图"):
        """绘制热力图"""
        pass

    def plot_scatter(self, x, y, labels, title="散点图"):
        """绘制散点图"""
        pass

    def plot_boxplot(self, data, labels, title="箱线图"):
        """绘制箱线图"""
        pass
```

**验收标准**:
- [ ] 5 种新图表类型实现
- [ ] 单元测试覆盖率 >= 90%
- [ ] 图表样式美观专业
- [ ] 支持中文显示

#### 2.2 报告主题系统（0.5 人日）

**文件**: `skills/mcda-core/lib/reports/themes.py`（新建）

**预定义主题**:
- `default`: 默认绿色主题
- `professional`: 专业蓝色主题
- `minimal`: 极简黑白主题
- `vibrant`: 鲜艳多彩主题
- `dark`: 深色主题

**验收标准**:
- [ ] 5 种预定义主题实现
- [ ] 主题配置 API 可用
- [ ] HTML 报告支持主题切换

#### 2.3 自定义报告模板（0.5 人日）

**文件**: `skills/mcda-core/lib/reports/templates.py`（新建）

**预定义模板**:
- `simple`: 简洁模板
- `detailed`: 详细模板
- `professional`: 专业模板

**验收标准**:
- [ ] Jinja2 模板引擎集成
- [ ] 3 种预定义模板
- [ ] 模板管理器实现

#### 2.4 交互式 HTML 报告（1 人日，可选）

**文件**: `skills/mcda-core/lib/reports/interactive_html.py`（新建）

**功能**:
- 图表缩放（使用 Plotly.js）
- 数据筛选和排序
- 实时权重调整（敏感性分析）

**验收标准**:
- [ ] 交互式图表可用
- [ ] 数据筛选功能正常
- [ ] 实时敏感性分析

---

### Phase 3: 高级可视化（2-3 人日）

#### 目标

实现敏感性分析可视化和决策路径追踪。

#### 3.1 敏感性分析热力图（1 人日）

**文件**: `skills/mcda-core/lib/visualization/sensitivity_heatmap.py`（新建）

**功能**:
- 权重敏感性可视化
- 排名变化热力图
- 交互式热力图（可选）

**验收标准**:
- [ ] 权重敏感性可视化实现
- [ ] 排名变化热力图生成
- [ ] 交互式热力图（可选）

#### 3.2 决策路径追踪图（1 人日）

**文件**: `skills/mcda-core/lib/visualization/decision_path.py`（新建）

**功能**:
- 决策流程图
- 桑基图（准则贡献）
- 关键节点标注

**验收标准**:
- [ ] 决策流程图生成
- [ ] 桑基图生成
- [ ] 关键节点标注

#### 3.3 Plotly Dash 集成（1 人日，可选）

**功能**:
- 交互式 Web 应用
- 实时参数调整
- 动态可视化

**验收标准**:
- [ ] Dash 应用可运行
- [ ] 参数调整界面
- [ ] 动态图表更新

---

### Phase 4: 集成测试和文档（1 人日）

#### 4.1 集成测试（0.5 人日）

**文件**: `tests/mcda-core/integration/test_v012_integration.py`

**验收标准**:
- [ ] 端到端测试通过
- [ ] 性能集成测试通过
- [ ] 兼容性测试通过

#### 4.2 文档更新（0.5 人日）

**文档清单**:

| 文档 | 路径 | 内容 |
|------|------|------|
| v0.12 发布说明 | `docs/active/mcda-core/v0.12/release-notes.md` | 新功能介绍 |
| 性能优化指南 | `docs/active/mcda-core/v0.12/performance-guide.md` | 性能优化建议 |
| 报告自定义指南 | `docs/active/mcda-core/v0.12/report-customization.md` | 报告自定义说明 |
| 可视化功能说明 | `docs/active/mcda-core/v0.12/visualization-guide.md` | 可视化功能文档 |

**验收标准**:
- [ ] 发布说明完整
- [ ] 用户指南清晰
- [ ] 示例代码可运行

---

## 📊 v0.12 验收标准

### 功能验收

#### Phase 1: 性能测试和优化
- [ ] 4 个规模测试通过（小/中/大/超大）
- [ ] 所有算法基准测试完成
- [ ] 性能报告生成（Markdown + JSON）
- [ ] 关键瓶颈已优化
- [ ] 性能提升 >= 20%

#### Phase 2: 报告进一步增强
- [ ] 5 种新图表类型实现
- [ ] 5 种预定义主题可用
- [ ] 自定义模板功能正常
- [ ] 交互式 HTML 报告（可选）

#### Phase 3: 高级可视化
- [ ] 敏感性分析热力图生成
- [ ] 决策路径追踪图生成
- [ ] Plotly Dash 集成（可选）

#### Phase 4: 集成测试和文档
- [ ] 集成测试通过
- [ ] 文档完整

### 质量验收
- [ ] 测试覆盖率 >= 90%
- [ ] 所有测试通过
- [ ] 代码符合 PEP 8
- [ ] 类型注解 100%

### 性能验收

| 场景 | 方案数 | 准则数 | 响应时间 | 内存 |
|------|--------|--------|----------|------|
| 小规模 | 10 | 5 | < 0.5s | < 50MB |
| 中规模 | 50 | 20 | < 2s | < 200MB |
| 大规模 | 200 | 50 | < 10s | < 1GB |
| 超大规模 | 1000 | 100 | < 60s | < 4GB |

---

## ⏱️ 工期估算

| Phase | 任务 | 工期 | 优先级 | 依赖 |
|-------|------|------|--------|------|
| **1** | 性能测试和优化 | | | |
| 1.1 | 性能测试框架搭建 | 0.5人日 | P0 | 无 |
| 1.2 | 大规模数据测试 | 1人日 | P0 | 1.1 |
| 1.3 | 算法性能基准测试 | 0.5人日 | P0 | 1.1 |
| 1.4 | 瓶颈分析和优化 | 1人日 | P0 | 1.2, 1.3 |
| **2** | 报告进一步增强 | | | |
| 2.1 | 更多图表类型 | 1人日 | P1 | 无 |
| 2.2 | 报告主题系统 | 0.5人日 | P1 | 无 |
| 2.3 | 自定义报告模板 | 0.5人日 | P2 | 2.2 |
| 2.4 | 交互式 HTML 报告 | 1人日 | P2 | 2.1, 2.2 |
| **3** | 高级可视化 | | | |
| 3.1 | 敏感性分析热力图 | 1人日 | P1 | 无 |
| 3.2 | 决策路径追踪图 | 1人日 | P2 | 无 |
| 3.3 | Plotly Dash 集成 | 1人日 | P2 | 3.1 |
| **4** | 集成测试和文档 | | | |
| 4.1 | 集成测试 | 0.5人日 | P0 | 1.4, 2.1, 3.1 |
| 4.2 | 文档更新 | 0.5人日 | P0 | 全部 |
| **最小版本** | Phase 1 + Phase 2.1-2.2 + Phase 4 | **5人日** | |
| **标准版本** | Phase 1 + 2.1-2.2 + 3.1 + Phase 4 | **6人日** | |
| **完整版本** | 所有 Phase | **10人日** | |

---

## 🚀 时间表

### Week 1（4 人日）

**Day 1-2**: Phase 1.1-1.2（性能测试框架 + 大规模测试）
- Day 1: 搭建性能测试框架，实现测试数据生成器
- Day 2: 执行大规模数据测试，收集基准数据

**Day 3**: Phase 1.3-1.4（基准测试 + 优化）
- 上午：算法性能基准测试
- 下午：瓶颈分析，开始优化实现

**Day 4**: Phase 2.1-2.2（图表扩展 + 主题系统）
- 上午：实现新图表类型
- 下午：实现主题系统

### Week 2（3-6 人日）

**Day 5**: Phase 2.3 + Phase 3.1（模板 + 敏感性可视化）
- 上午：自定义报告模板
- 下午：敏感性分析热力图

**Day 6**: Phase 3.2（决策路径追踪）
- 决策流程图和桑基图

**Day 7-8**: Phase 2.4 + Phase 3.3（可选功能）
- 交互式 HTML 报告
- Plotly Dash 集成

**Day 9**: Phase 4（集成测试和文档）
- 集成测试
- 文档更新

**Day 10**: 缓冲和发布准备
- 回归测试
- 发布准备

### 关键里程碑

- [ ] Day 2: 性能测试框架完成，基准数据建立
- [ ] Day 4: 性能优化完成，新图表和主题实现
- [ ] Day 6: 高级可视化完成
- [ ] Day 9: 集成测试通过，文档完整
- [ ] Day 10: v0.12 发布准备完成

---

## 🔧 技术方案

### 技术栈

| 类别 | 技术选型 | 说明 |
|------|---------|------|
| 性能测试 | pytest + psutil + cProfile | 性能测量和分析 |
| 矩阵运算 | NumPy | 向量化计算优化 |
| 缓存 | functools.lru_cache | 结果缓存 |
| 图表生成 | matplotlib | 静态图表 |
| 交互图表 | Plotly.js（可选） | 交互式可视化 |
| 模板引擎 | Jinja2 | 报告模板 |
| 主题系统 | 自定义 CSS | 主题样式 |

### 依赖库

```txt
# 已有依赖
matplotlib>=3.8.0
numpy>=1.24.0
openpyxl>=3.1.0

# 新增依赖（可选）
jinja2>=3.1.0           # 报告模板
plotly>=5.18.0          # 交互式图表（可选）
dash>=2.14.0            # Web 应用（可选）
```

### 架构设计

```
skills/mcda-core/lib/
├── performance/              # 性能模块（新增）
│   ├── __init__.py
│   ├── benchmark_reporter.py
│   └── utils.py
├── visualization/            # 可视化模块（扩展）
│   ├── __init__.py
│   ├── charts.py            # 扩展新图表类型
│   ├── sensitivity_heatmap.py  # 新增
│   └── decision_path.py     # 新增
├── reports/                  # 报告模块（扩展）
│   ├── __init__.py
│   ├── html_generator.py
│   ├── pdf_generator.py
│   ├── themes.py            # 新增
│   ├── templates.py         # 新增
│   └── templates/           # 新增目录
│       ├── simple.html
│       ├── detailed.html
│       └── professional.html
└── utils/                    # 工具模块（扩展）
    ├── array_ops.py         # 新增
    └── cache.py             # 新增

tests/mcda-core/
├── performance/              # 性能测试（新增）
│   ├── __init__.py
│   ├── test_large_scale.py
│   ├── test_benchmarks.py
│   └── fixtures/
└── integration/              # 集成测试（扩展）
    └── test_v012_integration.py
```

---

## 📝 风险管理

### 高风险项

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| 性能优化引入 Bug | 高 | 中 | 完整回归测试，小步迭代 |
| 大规模测试环境不足 | 中 | 低 | 使用云服务器或优化测试数据 |
| 交互式图表复杂度高 | 中 | 中 | 优先实现静态图表，交互式作为可选 |
| 工期超预期 | 高 | 中 | 按 P0/P1/P2 优先级分批交付 |

### 应急计划

**场景1: 性能优化效果不达标**
- 策略: 降低优化目标，确保无回归
- 触发条件: 优化后性能提升 < 10%

**场景2: 交互式功能开发延迟**
- 策略: 优先完成静态图表和主题系统，交互式功能延期
- 触发条件: Day 7 进度落后

**场景3: 工期超出 10 人日**
- 策略: 按 P0/P1/P2 优先级分批交付
- 最小版本: Phase 1 + Phase 4（4 人日）

---

## 📊 与原规划的对比

| 原规划 | 合并后 | 变化说明 |
|--------|--------|---------|
| v0.12: 性能测试和优化（3.5-4 人日） | Phase 1 | 保持不变 |
| v0.13: 报告进一步增强（2-3 人日） | Phase 2 | 合并入 v0.12 |
| v0.14: 高级可视化（2-3 人日） | Phase 3 | 合并入 v0.12 |
| - | Phase 4 | 新增集成测试和文档 |
| **总计**: 8-10 人日 | **总计**: 7-10 人日 | 工期基本一致，减少版本切换开销 |

### 合并优势

1. **开发效率提升**: 减少版本发布切换开销
2. **用户价值更高**: 一次性获得完整的性能+可视化体验
3. **测试协同**: 性能测试和可视化测试共享测试数据
4. **发布节奏**: 7-10 人日适合作为一个迭代周期

---

## 🎯 执行建议

### 推荐执行顺序

1. **Week 1**（4 人日）: 核心 Phase
   - Phase 1: 性能测试和优化（2.5 人日）
   - Phase 2.1-2.2: 图表扩展 + 主题系统（1.5 人日）

2. **Week 2**（2-6 人日）: 增强和可选 Phase
   - Phase 3.1-3.2: 高级可视化（2 人日）
   - Phase 2.3-2.4, 3.3: 可选功能（3 人日）
   - Phase 4: 集成测试和文档（1 人日）

### 最小可行版本（MVP）

如果工期紧张，优先实现：

- [ ] Phase 1: 性能测试和优化
- [ ] Phase 2.1: 新图表类型
- [ ] Phase 2.2: 主题系统
- [ ] Phase 4: 集成测试和文档

**工期**: 约 4-5 人日

### 完整版本

所有 Phase 完成，包括可选功能。

**工期**: 约 10 人日

---

## 🎓 技术亮点

### 1. 科学的性能测试方法

- 4 个规模级别（小/中/大/超大）
- 响应时间和内存双重标准
- 自动化性能测量工具

### 2. 丰富的图表类型

- 10+ 种图表类型（原有 4 种 + 新增 6 种）
- 支持 5 种预定义主题
- 支持自定义主题

### 3. 高级可视化能力

- 敏感性分析热力图
- 决策路径追踪图
- 可选的交互式可视化

### 4. 灵活的报告系统

- Jinja2 模板支持
- 主题切换
- 多种输出格式

---

**计划创建日期**: 2026-02-05
**计划创建人**: Claude (Opus 4.5)
**状态**: ✅ 待用户确认

---

## 总结

v0.12 是一个**功能增强与性能优化**的综合版本，包括：

1. **Phase 1: 性能测试和优化**（2.5 人日）
2. **Phase 2: 报告进一步增强**（2-3 人日）
3. **Phase 3: 高级可视化**（2-3 人日）
4. **Phase 4: 集成测试和文档**（1 人日）

**总工期**: 7-10 人日（最小版本 4-5 人日）

**关键目标**:
- ✅ 建立性能基准
- ✅ 支持大规模数据（1000 方案 × 100 准则）
- ✅ 性能优化 20%+
- ✅ 丰富的图表和主题
- ✅ 高级可视化能力
